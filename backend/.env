# Copy to .env and fill in
MODEL_PATH=./models/best_model.keras
META_PATH=./models/meta.json
INDEX_MAPPING_PATH=./models/100_index_mapping.json

# Inference
SEQ_LEN=60
MIN_CONFIDENCE=0.55
DEVICE=cpu

# CORS — Lovable preview + published URL
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080,http://localhost:8081,http://10.254.51.28:8080,http://10.254.51.28:8081,https://id-preview--62f2b937-3128-4897-85e5-86b7cad7f825.lovable.app

# Optional: shared secret Pi must send in WS query (?token=...)
PI_SHARED_TOKEN=change-me
